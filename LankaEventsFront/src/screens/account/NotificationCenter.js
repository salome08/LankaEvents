import { View, Text } from "react-native";
import { styles } from "../styles";

const NotificationCenter = () => {
  return (
    <View style={styles.container}>
      <Text variant="headlineMedium">Notification Center !</Text>
    </View>
  );
};

export default NotificationCenter;
